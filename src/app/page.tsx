import { LogoIcon } from '../assets';

const HomePage = () => {
  return (
    <div className='flex items-center justify-center text-3xl h-dvh font-medium'>
      <LogoIcon width={100} height={100} />
    </div>
  );
};

export default HomePage;
